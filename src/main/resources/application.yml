banner:
    location: classpath:/auxiliares/banner.txt
    
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/sigecu1?useSSL=false
    username: root
    password: root


  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
      naming:
        strategy: org.hibernate.cfg.ImprovedNamingStrategy
      
    
  thymeleaf:
    cache: false